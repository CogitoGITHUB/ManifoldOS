;; Disabled - audio packages broken
(define-module (substrate user-space root loaders audio)
  #:export (root-audio-packages root-audio-services))

(define-public root-audio-packages '())

(define-public root-audio-services '())